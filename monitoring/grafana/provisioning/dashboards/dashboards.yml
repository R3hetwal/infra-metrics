apiVersion: 1

providers:
  - name: "infra-metrics"
    orgId: 1
    folder: "AI Services"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
